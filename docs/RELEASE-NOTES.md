首次公开版本。

- Windows x64：下载 ZIP，完整解压后双击 `Start.cmd`，无需另外安装 Node.js。
- 浏览器首次配置向导：语音服务、可选推理后端、麦克风与播放测试。
- 配置保存在自己的私有数据目录，发行包不包含任何服务凭据。
- 支持 Docker 自部署和 Render 部署模板。
- 服务器模式有初始化口令和管理员密码保护。

请自行准备可用的实时语音模型部署和 API Key。运行中的语音会话及后端模型调用由所配置的服务商计费。
